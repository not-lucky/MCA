#include <bits/stdc++.h>

using namespace std;

int main() {
  vector<int> v;
  v.push_back(19);
  v.push_back(19);
  v.push_back(120);
  v.push_back(21);
  v.push_back(22);
  v.push_back(23);
  
  for (auto i = v.begin(); i != v.end(); i++) {
    cout << *i << endl;
  }
}